extends layout 

block content 
    h1 #{title}: #{bookinstancedeletion.id}
        p Do you really want to delete this Book Instance?
        form(method='POST' action='')
            div.form-group
                input#bookidinstance.form-control(type='hidden',name='bookidinstance', required='true', value=bookinstancedeletion._id )
            button.btn.btn-primary(type='submit') Delete
